es:
  activerecord:
    models:
      quotation: Cotización
    attributes:
      quotation:
        client: Cliente
        state: Estado
    errors:
      models:
        quotation:
          attributes:
            state:
              destroy_impossible: no permite la eliminación de la cotización
              activation_impossible: no permite la activación de la cotización
              approve_impossible: no permite la aprobación de la cotización
              expiration_impossible: no permite la expiración de la cotización